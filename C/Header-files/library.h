#ifndef library   // Included guard so if imported more then once it's only copied once 
#define library

//delecration of all of the functions in the library.c file 
float sum(float nums[], int size);

#endif